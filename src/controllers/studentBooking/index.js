const initiateBooking = require("./initiateBooking.controller");
const getMyBookings = require("./getMyBookings.controller");
const getOneMyBooking = require("./getOneMyBooking.controller");

module.exports = {
  initiateBooking,
  getMyBookings,
  getOneMyBooking,
};
