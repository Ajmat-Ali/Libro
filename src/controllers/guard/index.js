const createGuard = require("./createGuard.controller");
const getGuards = require("./getGuards.controller");
const deactivateGuard = require("./deactivateGuard.controller");

module.exports = {
  createGuard,
  getGuards,
  deactivateGuard,
};
