const ROLES = {
  OWNER: "owner",
  GUARD: "guard",
  STUDENT: "student",
};

const APPROVAL_STATUS = {
  PENDING: "pending",
  ACCEPTED: "accepted",
  REJECTED: "rejected",
};

module.exports = { ROLES, APPROVAL_STATUS };
